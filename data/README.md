# Data

- `raw/`: collected source records, never treated as trusted training data by themselves.
- `dataset/`: validated JSONL output.
- `logs/`: runtime logs.
- `state/`: future persistent source cursors and provenance state.

The agent records source URLs and domains. Review licensing before redistribution or commercial training.